#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@s-ui/lint/bin/sui-lint.js" "$@"
  ret=$?
else 
  node  "$basedir/../@s-ui/lint/bin/sui-lint.js" "$@"
  ret=$?
fi
exit $ret
