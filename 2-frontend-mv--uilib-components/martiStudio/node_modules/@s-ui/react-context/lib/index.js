import React from 'react';
import hoistNonReactStatics from 'hoist-non-react-statics';
var SUIContext = React.createContext();

SUIContext.wrapper = function (Component, displayName) {
  var WrappedComponent = function WrappedComponent(props) {
    return React.createElement(SUIContext.Consumer, null, function (context) {
      return React.createElement(Component, Object.assign({}, context, props));
    });
  };

  WrappedComponent.displayName = Component.displayName || displayName;
  return hoistNonReactStatics(WrappedComponent, Component);
};

export default SUIContext;