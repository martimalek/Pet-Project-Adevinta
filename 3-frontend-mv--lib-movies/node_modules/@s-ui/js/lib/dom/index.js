export var getCurrentElementFocused = function getCurrentElementFocused() {
  return document.activeElement;
};
export var getFocusedItemIndex = function getFocusedItemIndex(items) {
  var currentElementFocused = getCurrentElementFocused();
  var index = Array.from(items).findIndex(function (item) {
    return item === currentElementFocused;
  });
  return index === -1 ? null : index;
};