export var fromSnakeToCamelCase = function fromSnakeToCamelCase(string) {
  return string.replace(/(_\w)/g, function (m) {
    return m[1].toUpperCase();
  });
};
export var fromCamelToSnakeCase = function fromCamelToSnakeCase(string) {
  return string.replace(/([A-Z])/g, function (s) {
    return '_' + s.toLowerCase();
  });
};